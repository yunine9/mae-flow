方案和实现清单已经稳定，现在只确认开发节奏，不重新讨论需求。

先按模块、依赖关系和可独立编译的边界划分 1-6 个检查点；小改可只有 1 个，
常规任务建议 2-4 个。检查点应让一次 diff 容易读懂，不能按文件数或每个 commit
机械切分。执行：

`python "{MAEFLOW_PATH}" checkpoint plan --item "<CP1 业务边界>" [--item "<CP2 业务边界>" ...]`

命令会冻结写码前 HEAD、实现清单结构和检查点列表，并输出三个固定选项：

- `按检查点分阶段开发、推送和检视`：每批提交、compile-agent 通过、普通 push 后停下让用户检视；
- `一次完成全部代码，最终统一检视`：仍小步提交并按批编译，但中途不 push、不等待用户；
- `调整检查点划分`：不解锁源码，结合用户意见重新生成方案。

用户确认前禁止修改源码。月光宝盒自动旁路本步骤，保持无人值守。
