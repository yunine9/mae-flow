对照 `.mae-flow-work/{单号}/spec.md`、Grill 决策、Story 和最终代码执行规格符合性检查，不创建或修改 OpenSpec 文件。

按下方内嵌方法生成 `.mae-flow-work/{单号}/verification.md`，至少记录：已核对的验收条件、最终代码证据、异常与兼容性结论、未解决风险和总体结论。最后必须用独立一行写 `PASS` 或 `FAIL`；存在任何 `FAIL` 时不得完成本步。报告是本地过程件，禁止提交。

发现 Spec 与代码不一致时只向用户裁决一次：

- 本地 Spec 需要修订且实现正确：修订本地 Spec 后重做本步；
- 代码实现偏离：回到编码入口修复，并重新经过完整编译、CodeCheck、UT 和本检查；
- 需求本身仍不明确：记录问题并等待用户决定，禁止擅自选边。

验证通过并展示摘要后 done。编译、CodeCheck、UT 已由前序步骤提供真实证据，本步不重复执行。

──── 本步骤内嵌方法原文（已固定版本） ────
{{CAPABILITY_PACK:verify}}
