编译已经通过，但普通模式必须停在这里，让用户检视本轮评审修复代码后再决定是否继续。

先展示本轮检视收据中的提交范围和文件清单，再亲自阅读该范围的完整 `git diff`，逐条对应评审意见说明
实际修法、行为影响、风险点，并给出用户可以亲自执行的验证命令、步骤和预期结果。不能只报“编译通过”
或 diff stat。

然后用 AskUserQuestion 提供两个固定选项：

- `我已认真检视并完成自验证，继续` → 执行 `python "{MAEFLOW_PATH}" done --choice continue`
- `需要调整代码` → 执行 `python "{MAEFLOW_PATH}" done --choice revise`，回到评审修复环节修改、提交并重新编译

用户没有明确选择继续时必须停在本步。月光宝盒由状态机直接跳过本节点，不执行上述询问。
