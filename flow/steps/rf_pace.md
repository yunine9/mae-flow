评审意见已经逐条裁决。写码前按可一起修复、一起编译和一起检视的意见组合划分
1-4 个检查点，不要按每条意见机械拆成一次 push。

执行：

`python "{MAEFLOW_PATH}" checkpoint plan --item "<CP1 意见/模块范围>" [--item "<CP2 意见/模块范围>" ...]`

向用户展示命令生成的完整方案，并提供三个固定选项：

- `按检查点分阶段开发、推送和检视`
- `一次完成全部代码，最终统一检视`
- `调整检查点划分`

用户确认前禁止修改源码。月光宝盒自动旁路，不产生人工停顿。
