编译已经通过，但普通模式必须停在这里，不能立即进入后续检查，更不能替用户选择继续。

先把本轮检视收据中的提交范围和文件清单展示给用户，再亲自阅读该范围的完整 `git diff`，按文件说明：

1. 改了什么行为，为什么这样改；
2. 关键分支、异常路径和兼容性处理；
3. 你认为最值得用户重点检查的风险点；
4. 用户可以亲自执行的验证命令、操作步骤和预期结果。

不要只给“编译通过”或 diff stat；用户需要看懂本次实际改了哪些代码。也不要在本步催促用户快速确认。

展示完后用 AskUserQuestion 提供两个固定选项：

- `我已认真检视并完成自验证，继续` → 执行 `python "{MAEFLOW_PATH}" done --choice continue`
- `需要调整代码` → 执行 `python "{MAEFLOW_PATH}" done --choice revise`，回到编码环节修改、提交并重新编译

用户没有明确选择继续时必须停在本步。月光宝盒由状态机直接跳过本节点，不执行上述询问。
