修改范围已经确认。写第一行代码前，先把本次小改划成可独立编译、容易检视的
1-3 个检查点；确实只有一个原子改动时只设 CP1，不要为了形式强拆。

执行：

`python "{MAEFLOW_PATH}" checkpoint plan --item "<CP1 业务边界>" [--item "<CP2 业务边界>" ...]`

向用户展示命令生成的完整方案，并提供三个固定选项：

- `按检查点分阶段开发、推送和检视`
- `一次完成全部代码，最终统一检视`
- `调整检查点划分`

用户确认前禁止修改源码。月光宝盒自动旁路，不产生人工停顿。
